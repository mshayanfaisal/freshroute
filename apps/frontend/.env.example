# Frontend build-time config (Vite inlines these at build).
# Leave blank for local dev — requests use the Vite proxy to http://localhost:3000.
# For production, set to your deployed backend origin (NO trailing slash, NO /api):
#   VITE_API_URL=https://freshroute-api.onrender.com
VITE_API_URL=
